# Sourced by UWSM for the graphical session (export KEY=VAL).
# Prefer this over hl.env() in Hyprland when using UWSM.

export GTK_THEME=Adwaita:dark
export XCURSOR_SIZE=24
export PATH="${HOME}/bin:${PATH}"

# export STEAM_FORCE_DESKTOPUI_SCALING=1.25
# export GDK_SCALE=1.33
# export QT_SCALE_FACTOR=1.33
