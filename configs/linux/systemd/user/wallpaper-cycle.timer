[Unit]
Description=Rotate wallpaper every 30 minutes

[Timer]
# First timed rotation 30m after the timer starts; then every 30m after each run.
# Initial wallpaper is handled by awww-daemon ExecStartPost.
OnActiveSec=30min
OnUnitActiveSec=30min
Persistent=true

[Install]
WantedBy=timers.target
